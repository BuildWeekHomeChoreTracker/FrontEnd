import react from 'react';

const initialState = {
  users: [],
  isLogging: false,
  error: ''
};

export const loginReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
