const initialState = {
  user: {},
  isLogging: false,
  error: ''
};

export const childLoginReducer = (state = initialState, {type, payload}) => {
  switch (type) {
    default:
      return state;
  }
};
